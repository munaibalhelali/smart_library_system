#include <iostream>
#include <string>
#include "login_data.h"
using namespace std;

class Login{
  private:
   char admin, student, choice;
  
  public:
  Login( );

  int login()

  string to_json(logindaten input);
};


